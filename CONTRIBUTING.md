# 参与贡献

欢迎改进设计指引、安装器、使用文档和可复现案例。先描述具体问题：在哪个任务、组件或安装环境中，当前行为与预期有何差异。

## 在哪里修改

| 改动 | 维护位置 |
| --- | --- |
| Skill 的触发条件、工作流与设计指导 | `skills/apple-design-skill/SKILL.md` |
| Codex 展示信息与默认提示 | `skills/apple-design-skill/agents/openai.yaml` |
| CLI 参数与输出 | `bin/apple-design-skill.mjs` |
| 安装、缓存、回退与卸载 | `lib/installer.mjs` |
| 快速开始与产品介绍 | `README.md` |
| 安装、版本与发布细节 | `docs/` |
| 使用 Skill 的可运行案例与最终截图 | `examples/` |

可安装技能只有一个源目录。不要把开发文档、截图或演示页面塞进技能目录，也不要手工维护多套相同指令。插件清单只是分发元数据；有变化时与包名、版本及实际能力保持一致。

## 本地验证

需要 Node.js 22+，没有运行时依赖。从仓库根目录运行：

```sh
npm ci --ignore-scripts
npm run check
npm run check:example
npm test
npm pack --dry-run
```

`check` 验证脚本、文档本地链接及包、锁文件和插件清单的一致性；`check:example` 验证页面入口、截图格式、本地资源与脚本语法；`test` 在临时目录覆盖安装、回退、修改保护和真实 npm 压缩包的安装。

测试安装使用临时项目，避免在本仓库留下覆盖当前技能源的旧安装副本。仓库会忽略本技能的项目安装目录和版本缓存；其他项目技能仍可正常跟踪。

只修改文档时，核对链接、代码示例和引用的截图。修改交互时，验证真实用户操作和状态；截图应对应实际运行结果。安装器改动应补充能复现问题的行为测试。

## 修改设计指引

说明现有指导在哪个具体场景失效，再提交最小的规则修正。保留用户需求、现有技术栈与设计系统的优先级；避免把某个案例的颜色、圆角或布局变成所有项目的硬性要求。

涉及下拉菜单外观时，提供关闭和展开两种状态，验证选中与活动行、键盘导航、关闭方式和真实提交值。涉及手势时，再检查速度衔接、取消与中断。

## 添加或更新对照案例

示例目录只保留可运行的最终页面、必要资源、效果截图与简短使用说明。页面变更后同步更新截图，确保展示与实际结果一致；无需保存需求草稿、指令快照或过程清单。

对比说明聚焦共享功能和可观察的设计差异，不把视觉偏好写成已验证的效果提升。

提交说明应包含具体变化、原因、验证结果与剩余限制。发布维护见[发布指南](docs/releasing.md)。
